{
    "pull-request-title-pattern": "chore(${component}): community release ${version}",
    "packages": {
        "operator": {
            "changelog-path": "operator/CHANGELOG.md",
            "component": "operator"
        }
    }
}